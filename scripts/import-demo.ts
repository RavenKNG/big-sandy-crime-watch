import records from"../fixtures/demo-records.json";import{JsonFixtureImporter}from"../src/lib/importers";import type{DemoRecord}from"../src/lib/types";async function main(){const importer=new JsonFixtureImporter(records as DemoRecord[]);const imported=await importer.import();console.log(JSON.stringify({mode:process.argv.slice(2),source:importer.name,recordsCreated:imported.length,publishStatus:imported[0]?.publishStatus},null,2))}void main();
