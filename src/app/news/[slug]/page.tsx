import{notFound}from"next/navigation";import{AdSlot}from"@/components/AdSlot";import{formatDate,getArticle}from"@/lib/content";export default async function NewsPage({params}:{params:Promise<{slug:string}>}){const a=getArticle((await params).slug);if(!a)notFound();return <main><article className="content-card"><p className="eyebrow">{a.category} - {formatDate(a.publishedAt)}</p><h1>{a.title}</h1><p>{a.summary}</p><AdSlot placement="article-top"/><p>{a.body}</p><p className="muted">Source: {a.sourceName}</p></article></main>}
