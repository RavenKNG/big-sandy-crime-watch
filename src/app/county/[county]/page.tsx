import{RecordCard}from"@/components/RecordCard";import{getPublishedRecords}from"@/lib/content";export default async function CountyPage({params}:{params:Promise<{county:string}>}){const county=(await params).county;const records=getPublishedRecords().filter(r=>r.county.toLowerCase()===county.toLowerCase());return <main><p className="eyebrow">COUNTY DESK</p><h1>{county} County</h1><div className="record-grid">{records.map(r=><RecordCard key={r.slug} record={r}/>)}</div></main>}
