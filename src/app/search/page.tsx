import{RecordCard}from"@/components/RecordCard";import{getPublishedRecords}from"@/lib/content";export default function SearchPage(){return <main><p className="eyebrow">SEARCH DEMO</p><h1>Browse synthetic records</h1><p>Search controls become database-backed after PostgreSQL setup.</p><div className="record-grid">{getPublishedRecords().map(r=><RecordCard key={r.slug} record={r}/>)}</div></main>}
