import type{MetadataRoute}from"next";import{articles,demoRecords}from"@/lib/demo-data";import{categories,counties}from"@/lib/content";const base="https://BigSandyCrimeWatch.com";export default function sitemap():MetadataRoute.Sitemap{return[{url:base},{url:`${base}/disclaimer`},{url:`${base}/correction-request`},...articles.map(a=>({url:`${base}/news/${a.slug}`})),...demoRecords.filter(r=>r.publishStatus==="PUBLISHED").map(r=>({url:`${base}/records/${r.slug}`})),...counties.map(c=>({url:`${base}/county/${c}`})),...categories.map(c=>({url:`${base}/category/${c}`}))]}
