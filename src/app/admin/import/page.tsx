import { readFile } from "node:fs/promises";import { join } from "node:path";import { officialSourceAdapterStatus } from "@/lib/importers";import type { MappingReport } from "@/lib/adapters/bsrdc-mapping-report";
export const dynamic="force-dynamic";
async function getReport(name:string){try{return JSON.parse(await readFile(join(process.cwd(),"work",name),"utf8"))as MappingReport}catch{return undefined}}
export default async function ImportPage(){const[fileReport,fixtureReport]=await Promise.all([getReport("bsrdc-file-mapping-report.json"),getReport("bsrdc-mapping-report.json")]);const report=fileReport??fixtureReport;return <main><section className="content-card"><p className="eyebrow">VALIDATED LOCAL IMPORT</p><h1>CSV and JSON fixture import</h1><p>Imports are deduplicated and normalized to <strong>DRAFT</strong>. Use local sanitized fixture files only.</p><pre>npm run import:csv -- --file fixtures/demo-records.csv{"\n"}npm run import:demo -- --file fixtures/demo-records.json{"\n"}npm run import:reviewed-fixture{"\n"}npm run bsrdc:map-dry-run -- --from YYYY-MM-DD --to YYYY-MM-DD{"\n"}npm run bsrdc:map-file -- --file path-to-human-reviewed-local-snapshot.json</pre><h2>BSRDC mapping dry-run</h2><p className="notice">Dry-run mapping does not write to the database, publish records, download images, or queue Facebook posts.</p><p>Adapter status: <span className="pill">{officialSourceAdapterStatus.enabled?"enabled":"disabled"}</span></p><p>{officialSourceAdapterStatus.message}</p>{report?<div className="admin-card"><h3>Last {fileReport?"file-":""}mapping report</h3><p>{report.mode} - generated {report.generatedAt}</p><p>{report.rowsFound} rows, {report.detailPagesDetected} detail references, {report.chargeRows} charge rows.</p><p>Image status: {report.imageStatus}</p><p>Warnings: {report.warnings.length}</p></div>:<p>No mapping report generated on this server.</p>}<h2>Reviewed BSRDC fixture mapping</h2><p>The reviewed synthetic fixture command validates the mapping and saves admin-review drafts only. It does not enable live fetching, publish records, or queue Facebook posts.</p><h2>CSV columns</h2><p>slug, displayName, county, recordDate, status, sourceName, sourceTimestamp, offense, statute, chargeDescription</p><p className="notice">Real official-source fetching remains disabled pending human review.</p></section></main>}
