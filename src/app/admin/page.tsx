import Link from "next/link";import { updateCorrectionStatus } from "@/app/actions";import { articles,demoRecords } from "@/lib/demo-data";import { getDb } from "@/lib/db";import { officialSourceAdapterStatus } from "@/lib/importers";
export const dynamic="force-dynamic";
export default async function AdminPage(){const db=getDb();const [records,corrections]=await Promise.all([db.publicRecordDemo.findMany({orderBy:{createdAt:"desc"},take:10}),db.correctionRequest.findMany({orderBy:{createdAt:"desc"},take:10})]);return <main><p className="eyebrow">EDITORIAL CONTROL ROOM</p><h1>Admin dashboard</h1><div className="admin-grid"><section className="admin-card"><h2>Review queue</h2><p>{records.filter(r=>r.publishStatus==="DRAFT").length} saved synthetic drafts.</p><Link className="button" href="/admin/manual-entry">Create draft</Link></section><section className="admin-card"><h2>Import preview</h2><p>Validated JSON and CSV demo import commands are ready.</p><Link className="button" href="/admin/import">Open importer</Link></section><section className="admin-card"><h2>Facebook drafts</h2><p>Manual export is ready. API posting remains disabled.</p><Link className="button" href="/admin/facebook-export">Open export</Link></section><section className="admin-card"><h2>Sponsors</h2><p>Saved sponsors remain disabled by default.</p><Link className="button" href="/admin/sponsors">Sponsor setup</Link></section></div><h2>Saved synthetic records</h2>{records.map(r=><p key={r.id}><Link href={`/admin/records/${r.slug}`}>{r.displayName}</Link> - {r.publishStatus}</p>)}<h2>Correction requests</h2>{corrections.length===0?<p>No requests submitted.</p>:corrections.map(r=><section className="admin-card" key={r.id}><strong>{r.requestType} - {r.status}</strong><p>{r.name} - {r.email}</p><p>Submitted: {r.createdAt.toISOString()}</p>{r.relatedUrl&&<p><a href={r.relatedUrl}>Open related record</a></p>}<p>{r.message}</p><form action={updateCorrectionStatus}><input type="hidden" name="id" value={r.id}/><label>Review status<select name="status" defaultValue={r.status}><option>NEW</option><option>REVIEWING</option><option>RESOLVED</option><option>DENIED</option></select></label><button type="submit">Update request</button></form></section>)}<h2>System status</h2><div className="admin-grid"><section className="admin-card"><h3>Articles</h3><p>{articles.length} editorial demo posts and {demoRecords.length} bundled fixtures.</p></section><section className="admin-card"><h3>Official-source adapter</h3><p>{officialSourceAdapterStatus.message}</p><span className="pill">disabled</span></section></div></main>}
