import Link from "next/link";
import { articles, demoRecords } from "@/lib/demo-data";
import { officialSourceAdapterStatus } from "@/lib/importers";

export default function AdminPage() {
  return <main><p className="eyebrow">EDITORIAL CONTROL ROOM</p><h1>Admin dashboard demo</h1><p>Operator-ready demo surfaces are available below. Database persistence and authentication remain deployment setup work.</p><div className="admin-grid"><section className="admin-card"><h2>Review queue</h2><p>{demoRecords.filter((record) => record.publishStatus === "DRAFT").length} synthetic fixture awaiting review.</p><Link className="button" href="/admin/manual-entry">Create draft</Link></section><section className="admin-card"><h2>Import preview</h2><p>Validated JSON and CSV demo import commands are ready.</p><Link className="button" href="/admin/import">Open importer</Link></section><section className="admin-card"><h2>Facebook drafts</h2><p>Manual export is ready. API posting remains disabled.</p><Link className="button" href="/admin/facebook-export">Open export</Link></section><section className="admin-card"><h2>Sponsors</h2><p>Ads stay disabled until local sponsor details are configured.</p><Link className="button" href="/admin/sponsors">Sponsor setup</Link></section></div><h2>System status</h2><div className="admin-grid"><section className="admin-card"><h3>Articles</h3><p>{articles.length} editorial demo posts.</p><span className="pill">review workflow ready</span></section><section className="admin-card"><h3>Official-source adapter</h3><p>{officialSourceAdapterStatus.message}</p><span className="pill">disabled</span></section><section className="admin-card"><h3>Safeguards</h3><p>Free correction, hide, and de-index requests. No paid removal workflow. No home-address publishing.</p><span className="pill">required</span></section></div></main>;
}
