import Link from"next/link";import{formatDate}from"@/lib/content";import type{DemoRecord}from"@/lib/types";export function RecordCard({record}:{record:DemoRecord}){return <article className="record-card"><div className="image-placeholder">DEMO<br/>RECORD</div><div><p className="eyebrow">{record.county} County - {formatDate(record.recordDate)}</p><h3>{record.displayName}</h3><p>{record.charges[0]?.chargeDescription}</p><p className="count">{record.charges.length} listed demo charge{record.charges.length===1?"":"s"}</p><Link className="button" href={`/records/${record.slug}`}>View Details</Link></div></article>}
